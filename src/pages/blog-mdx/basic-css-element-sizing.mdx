import { blogMeta } from '../../blogs/meta.ts';

export const meta = blogMeta['basic-css-element-sizing'];

# A Memo For Basic CSS Element Sizing

Below is an example of JSX embedded in Markdown.

**Try and change the background color!**

<div style={{ padding: '20px', backgroundColor: 'tomato' }}>
  <h3>This is JSX</h3>
</div>
